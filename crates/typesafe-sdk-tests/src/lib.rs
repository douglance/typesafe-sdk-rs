//! Cross-crate conformance tests.
//!
//! The library is empty; everything lives in `tests/`. The crate exists so
//! tests that need the whole stack assembled — and the ones that talk to the
//! real service — have somewhere to live that no published crate depends on.
